#include<stdio.h>
int sfrmbeg(int arr[],int len){
    int i=0,j=len-1;
    int sum= arr[0];
    i=1;
    while(i<j){
        if(arr[i]>)
    }
}
int sfrmend()
int main()
{
    int test=0;
    scanf("%d",&test);
    while(test--){
        int len =0,i=0,j=0;
        scanf("%d",&len);
        int arr[len];
        for(i=0;i<len;i++)
            scanf("%d",&arr[i]);
        i=0;
        j=len-1;
    }
    return 0;
}
